# 007-r02 — Search Screens v2 (Players / Guilds / Bases)

> **Revision of 007 (rejected).** What was wrong: 007 kept the sidebar-era
> composition (search box over per-panel-QSS tree, bulk bar below, results in
> right dock). What changes: composition moves to the 023 table strategy
> (ribbon search, full-bleed dense table, footer context strip, selection in
> tray). What is preserved: all data contracts, context menus, bulk dialogs,
> guild-member splitter, sort/filter semantics.

## New composition (per screen)

```
ribbon: SEARCH PLAYERS · zone World          [search field][count][controls-space]
┌──────────────────────────────────────────────────────────────┐
│ full-bleed dense table (28px rows, rule headers)             │
├──────────────────────────────────────────────────────────────┤
│ footer: [n selected · n rows]        [bulk actions →]        │
└──────────────────────────────────────────────────────────────┘
```

Guilds: two stacked tables (guilds / members) keep the vertical splitter,
each with ribbon mini-title; selection of a guild fills members (existing
behavior). Bases: single table.

## Required functionality (inventory from code)

- Players: 8 columns, leader `[L]` prefix, sort keys, context menu (rename,
  delete, guild assign, deep links to editor/inventory), 4 bulk dialogs
  (item/pal/technology/guild-assign) — all preserved.
- Guilds: 4 columns + member sub-table (6 columns), context menus preserved.
- Bases: 4 columns, context menu preserved.
- Selection drives tray (facade set_player/set_guild/set_base) — unchanged.

## Implementation

Via 023 tasks 2–4 (main_window builders + SearchPanel). Tests: smoke search
screens (existing recipe) + full suite. Visual: code-based; captures in
Logs; screenshot QA PENDING.
