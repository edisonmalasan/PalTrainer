# 014-r02 — Exclusions v2

> **Revision of 014 (frozen).** Absorbed into 023 task 4: three side-by-side
> SearchPanels replaced by a single ribbon + segmented control
> (Players/Guilds/Bases) switching one full-bleed table; context menus and
> load/save exclusions flow preserved; add/remove via existing handlers.
> Tests: smoke switch + add/remove; suite. Visual PENDING.
