# 011-r02 — Player Inventory v2
> **STATUS (2026-09-04): IMPLEMENTED (tab shell).** Ribbon with player selector
> landed; header action buttons tokenized (pageSwitchBtn/toolButton grammar);
> centered title bar retired. Grid internals preserved.

> **Revision of 011 (frozen).** Wrong: 011 kept old inv_tabs layout (tabs
> top, equip slot grid, item grids below). New: **equipment workbench** with
> loadout-first hierarchy.

## New composition

```
ribbon: PLAYER INVENTORY · zone Edit   [player combo][stats delta chip]
left (~340px): LOADOUT ledger
  - equip slots as labeled ledger rows (icon + name + qty), click = picker
  - drops/buffs quick rows
center: CONTAINER pages (stacked QStackedWidget, segmented control)
  - common storage / key items / weapons / armor / material grids
right (~300px): ITEM INSPECTOR
  - selected item details (mono ids, qty spin, move/drop actions)
footer: [unlock map] [modify slots] [detection report chip]
```

## Required functionality (from inventory_tab.py, 4227 ln)

Player combo + `clear_player/refresh`, item pickers (qty/slots/loadout),
equip double-click, unlock-all-map signal (`unlock_all_map_requested`),
modify slots, overfill detection, stats save on change (`_on_stats_changed`
→ `_save_stats_to_raw_data`), drop overlay contract. Grids keep rarity/data
colors. All handlers preserved; presentation only.

## Implementation

Re-layout `inventory_tab.py` `_setup_ui` into the three-zone workbench;
`inv_tabs` → segmented control (`#pageSwitch` grammar from band reuse);
slot styles from styles.py constants → builder rules. Tests: smoke (build,
pick item, stats change), full suite. Visual PENDING.
