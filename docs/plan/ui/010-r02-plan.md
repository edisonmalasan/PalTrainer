# 010-r02 — Pal Editor v2
> **STATUS (2026-09-04): IMPLEMENTED (tab shell).** Ribbon with player selector
> landed; centered title bar and inline QSS retired. Workbench inner-zone
> recomposition of pal_editor_widget remains optional polish (palette done).

> **Revision of 010 (frozen).** Wrong: 010 reskinned the old palbox/party
> slot panel + right editor form. New: **workbench composition** — specimen
> view left, dense attribute ledger right, palbox strip bottom.

## New composition

```
ribbon: PAL EDITOR · zone Edit   [player combo][box nav ‹ 1/15 ›][search]
left (workbench, ~380px): SPECIMEN
  - pal header: name + title token, level chip (amber), rank stars (data colors)
  - stats ledger (HP/ATK/DEF/SPD mono rows w/ sliders + spin, bounds per
    pal-trainer-pal-editor skill)
  - passive skills chips (rarity colors) → SkillPicker (022 grammar)
  - active skills list → SkillPicker
bottom strip: PALBOX — 960 slots as compact grid pages (existing PartySlot
  widgets restyled: hairline slots, selection ring amber, right-click menu
  preserved; double-click add flow preserved)
right (remaining width): DETAILS ledger
  - identity (guid mono, instance, nickname, xp)
  - work/health/happiness rows; owner/guild/location readouts (mono)
```

## Required functionality (from editor/pal_editor + tab audit)

Party/palbox placement, create dialogs (create_dialogs), level popup,
stat editing bounds, skill pickers (blocking processEvents loop preserved),
GPS editor, bulk ops (max/fix/restore), dps/lifetime handling rules
(party-slot widget lifetime — §9 invariants), refresh/clear_player.

## Implementation

`pal_editor_tab.py` shell re-layout + `_PAL_STYLESHEET` removal (inline
block → builder rules `#specimenPanel`, `#palboxStrip`, `#palSlot`);
editor widgets tokenized. Tests: smoke (build, select slot, open pickers),
full suite + slow (save roundtrips). Visual PENDING.
