# 015-r02 — Docs (Wiki) v2 & Tab Guide

> **Revision of 015 (frozen).** New: **reference shelf** composition.

## New composition

```
ribbon: DOCS · zone Reference      [search field (filters topics)]
left (~280px): TOPIC SHELF (grouped list: guides / tabs / faq)
right: READER (scrollable rich text, Hanken headings, Inter body,
  mono for paths/keys) — replaces centered browser pane
```

Tab Guide dialog (`tab_guide_dialog.py`) restyled to 022 sheet grammar with
the same per-tab content and menu access point (band utilities → guide).
Content files under `resources/` untouched. Tests: smoke (topic switch,
search filter), suite. Visual PENDING.
