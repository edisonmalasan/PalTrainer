# 013-r02 — JSON Editor v2

> **Revision of 013 (frozen).** New: **inspection desk** composition.

## New composition

```
ribbon: JSON · zone Edit    [file chip (mono)][size][read-only badge]
left (~300px): NODE TREE (structure navigation, mono keys)
right: PAYTEXT VIEW (QPlainTextEdit read-only-by-default, mono 12px)
footer: [find][goto path][copy][export] — write actions behind the
  read-only gate: preview + strict diff + explicit confirm (§9 invariant)
```

## Required functionality

`refresh()` on load, read-only default with explicit unlock flow, preview +
confirmation before any write, path validation. Search within text.
Unchanged behavior; new presentation. Tests: smoke (refresh, read-only
enforcement), suite. Visual PENDING.
