# 009-r02 — Base Inventory v2
> **STATUS (2026-09-04): IMPLEMENTED.** Ribbon + segmented view switch + context row
> landed; per-button inline QSS retired; per-tab theme override removed.
> Item-grid internals (RarityBorderDelegate, pickers) preserved as planned.

> **Revision of 009 (frozen).** Wrong: 009 kept pane-per-pane old layout
> (container list pane → item grids, guild pickers dialog-led). New: master–
> detail ledger composition.

## New composition

```
ribbon: BASE INVENTORY · zone World    [guild combo][base combo][economy chip]
left rail (280px): GUILD ledger
  - guild rows (name, level, member count) w/ inline actions (structure,
    economy, assign) via context menu; search filter on top
right: BASE detail
  - base header strip: base id (mono), area range, clone/export/import buttons
  - container tabs (tokenized QTabBar): chest/palbox/other
  - item grid: RarityBorderDelegate retained (game-data colors), qty badges
economy stats → footer strip (mono) + tray metrics
```

## Required functionality (from base_inventory_tab.py, 4176 ln)

Guild/base selection flows (`_clear_guild_selection` contract), container
list→item grids, item pickers/quantity/slots dialogs (022 grammar), guild
structure/economy dialogs, clone/export/import, area range update, audit
(`get_last_import_audit`), refresh/labels. All preserved; only presentation
moves. Save mutation stays in managers (§9 invariants).

## Implementation

Pane-by-pane with compile+smoke between (per original 009 discipline).
QSS via shared tokens; RarityBorderDelegate untouched (data contract).
Tests: smoke (selection, grid fill, dialogs open), full suite. Visual PENDING.
