# 012-r02 — Breeding v2

> **Revision of 012 (frozen).** Wrong: 012 kept old two-panel breeding calc
> layout. New: **breeding lab** composition.

## New composition

```
ribbon: BREEDING · zone Reference      [combo A][×][combo B][= result chip]
center: RESULT STAGE
  - offspring card: species name (Hanken title), rank probability bars,
    unique-combination notice (special color), traits list (data colors)
left: parent selectors as ledger rows w/ pal pickers (select-pal flow
  preserved; blocking picker loop untouched)
right: MATRIX drawer — compact probability table (mono) in a 023-style
  dense table, copy button
footer: exclusions notice (breeding exclusions list source, read-only)
```

## Required functionality (from breeding_tab.py audit)

Two pal selectors, calculation via breeding formulas skill (average rank +
tiebreakers + unique combos), result presentation, refresh/labels, deep
links to pal editor. Calculations untouched — presentation only.

## Implementation

Re-layout `breeding_tab.py`; result stage + matrix rules in builder.
Tests: smoke (select two pals, result updates), full suite. Visual PENDING.
