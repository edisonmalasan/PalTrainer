# 008-r02 — Map Viewer v2

> **Revision of 008 (frozen).** Wrong: 008 restyled the old layout (left
> sidebar tabs `#mapSidebarTabs`, map canvas right, results dock). New: canvas-
> first composition honoring Deck Operations — the map IS the canvas.

## New composition

```
ribbon: MAP · zone Load & Inspect     [layer chips: markers/zones/fog] [zoom %]
full-bleed interactive canvas (entire page minus ribbon)
left-floating mini legend card (surface_raised, collapsible):
  base markers legend, zone filters, coordinates readout (mono)
bottom-floating GPS strip (mono coords + copy) — replaces sidebar readouts
```

## Required functionality (from map_tab.py audit)

- Map browser/list of maps (generation + switching) → becomes ribbon layer
  dropdown (styled combo) + legend "Maps" section.
- Markers/zones/fog rendering, fog-of-war restore tool entry, deep-link to
  bases; click → tray selection (base); zoom/pan preserved.
- `refresh()` and `refresh_labels()` contracts unchanged; detached map window
  (`#detachedMapWindow`) restyle only.

## Implementation

Re-layout `map_tab.py` shell (canvas, ribbon, floats); keep
`map_generator.py` and all canvas painting untouched. QSS: `#mapLegendCard`,
`#mapGpsStrip`, layer chips reuse campaignStep grammar. Tests: smoke builds,
refresh ok, grab. Visual PENDING.
