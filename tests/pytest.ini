[pytest]
testpaths = tests
norecursedirs = ib node_modules dist .venv
python_files = test_*.py
addopts = -m "not slow" --deep-audit --strict-paths
markers =
    slow: slow integration tests (save file I/O, compression roundtrips)
